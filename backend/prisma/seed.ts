import { PrismaClient, RevisionType, RoleName, VideoProvider } from '@prisma/client'; import * as argon2 from 'argon2';
const prisma = new PrismaClient();
async function main() { for (const name of Object.values(RoleName)) await prisma.role.upsert({where:{name},update:{},create:{name}}); const email=process.env.SEED_ADMIN_EMAIL; const password=process.env.SEED_ADMIN_PASSWORD; if(email&&password){const user=await prisma.user.upsert({where:{email},update:{},create:{email,passwordHash:await argon2.hash(password)}}); const role=await prisma.role.findUniqueOrThrow({where:{name:RoleName.SUPER_ADMIN}}); await prisma.userRole.upsert({where:{userId_roleId:{userId:user.id,roleId:role.id}},update:{},create:{userId:user.id,roleId:role.id}})} const exam=await prisma.exam.upsert({where:{slug:'neet-ug'},update:{name:'NEET UG'},create:{name:'NEET UG',slug:'neet-ug'}}); for(const name of ['Physics','Chemistry','Biology']){const subject=await prisma.subject.upsert({where:{examId_slug:{examId:exam.id,slug:name.toLowerCase()}},update:{},create:{examId:exam.id,name,slug:name.toLowerCase()}}); for(const level of ['11','12']) await prisma.academicClass.upsert({where:{subjectId_slug:{subjectId:subject.id,slug:`class-${level}`}},update:{},create:{subjectId:subject.id,name:`Class ${level}`,slug:`class-${level}`}}); if(name==='Physics'){const cls=await prisma.academicClass.findUniqueOrThrow({where:{subjectId_slug:{subjectId:subject.id,slug:'class-11'}}});const chapter=await prisma.chapter.upsert({where:{classId_slug:{classId:cls.id,slug:'units-and-measurements'}},update:{},create:{classId:cls.id,name:'Units and Measurements',slug:'units-and-measurements'}});const topic=await prisma.topic.upsert({where:{chapterId_slug:{chapterId:chapter.id,slug:'physical-quantities'}},update:{},create:{chapterId:chapter.id,name:'Physical Quantities',slug:'physical-quantities'}});await prisma.video.upsert({where:{slug:'physical-quantities-introduction'},update:{},create:{title:'Physical Quantities: Introduction',slug:'physical-quantities-introduction',examId:exam.id,subjectId:subject.id,academicClassId:cls.id,chapterId:chapter.id,topicId:topic.id,provider:VideoProvider.LOCAL,providerAssetId:'https://example.com/physical-quantities.mp4',durationSeconds:600,isPublished:true,isFree:true}});await prisma.video.upsert({where:{slug:'physical-quantities-draft'},update:{},create:{title:'Physical Quantities: Draft',slug:'physical-quantities-draft',examId:exam.id,subjectId:subject.id,academicClassId:cls.id,chapterId:chapter.id,topicId:topic.id,provider:VideoProvider.LOCAL,providerAssetId:'https://example.com/draft.mp4',durationSeconds:480,isPublished:false,isFree:true}});await prisma.revisionItem.upsert({where:{id:'seed-formula-units'},update:{},create:{id:'seed-formula-units',title:'SI unit reminder',type:RevisionType.FORMULA,content:'Use SI base units consistently.',examId:exam.id,subjectId:subject.id,academicClassId:cls.id,chapterId:chapter.id,topicId:topic.id,isPublished:true}});await prisma.revisionItem.upsert({where:{id:'seed-note-units'},update:{},create:{id:'seed-note-units',title:'Physical quantities note',type:RevisionType.SHORT_NOTE,content:'Every measurement has a magnitude and a unit.',examId:exam.id,subjectId:subject.id,academicClassId:cls.id,chapterId:chapter.id,topicId:topic.id,isPublished:true}})}}} main().finally(()=>prisma.$disconnect());
