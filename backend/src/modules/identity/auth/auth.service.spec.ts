import { ConflictException, UnauthorizedException } from '@nestjs/common';
import { RoleName } from '@prisma/client';
import { AuthService } from './auth.service';
jest.mock('argon2', () => ({ hash: jest.fn(async () => 'hash'), verify: jest.fn(async (_: string, value: string) => value === 'correct') }));
describe('AuthService', () => { const cfg={getOrThrow:jest.fn((k:string)=>k.includes('EXPIRES')?'15m':'x'.repeat(32))} as any; const jwt={signAsync:jest.fn(async()=> 'token'),verifyAsync:jest.fn()} as any; const db:any={user:{findUnique:jest.fn(),create:jest.fn()},role:{findUniqueOrThrow:jest.fn()},refreshSession:{create:jest.fn(),findUnique:jest.fn(),update:jest.fn(),updateMany:jest.fn()}}; const service=new AuthService(db,jwt,cfg); beforeEach(()=>jest.clearAllMocks()); it('registers only a student role',async()=>{db.user.findUnique.mockResolvedValue(null);db.role.findUniqueOrThrow.mockResolvedValue({id:'student'});db.user.create.mockResolvedValue({id:'u',roles:[{role:{name:RoleName.STUDENT}}]});await service.register({email:'s@test.com',password:'correct',fullName:'Student'});expect(db.user.create.mock.calls[0][0].data.roles.create.roleId).toBe('student');}); it('rejects duplicate email',async()=>{db.user.findUnique.mockResolvedValue({id:'u'});await expect(service.register({email:'s@test.com',password:'correct',fullName:'Student'})).rejects.toBeInstanceOf(ConflictException);}); it('rejects invalid login and inactive users',async()=>{db.user.findUnique.mockResolvedValue({isActive:false,passwordHash:'h'});await expect(service.login({email:'x@test.com',password:'bad'})).rejects.toBeInstanceOf(UnauthorizedException);}); });
