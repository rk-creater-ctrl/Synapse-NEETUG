import { Body, Controller, Get, Param, Patch, Post, Query, UseGuards } from '@nestjs/common'; import { ApiBearerAuth, ApiTags } from '@nestjs/swagger'; import { RoleName } from '@prisma/client'; import { JwtAuthGuard } from '../../shared/guards/jwt-auth.guard'; import { Roles } from '../../shared/decorators/roles.decorator'; import { RolesGuard } from '../../shared/guards/roles.guard'; import { AcademicsService } from './academics.service'; import { AcademicDto,ListDto } from './dto'; const resources=['exams','subjects','classes','chapters','topics','subtopics'] as const; type Resource=typeof resources[number];
@ApiTags('academics')@Controller('academics')export class AcademicsController{constructor(private s:AcademicsService){} @Get(':resource')list(@Param('resource')r:Resource,@Query()q:ListDto){return this.s.list(r,q)} @Get(':resource/:id')get(@Param('resource')r:Resource,@Param('id')id:string){return this.s.get(r,id)}}
@ApiTags('admin academics')@ApiBearerAuth()@UseGuards(JwtAuthGuard,RolesGuard)@Roles(RoleName.ADMIN,RoleName.SUPER_ADMIN)@Controller('admin/academics')export class AdminAcademicsController{constructor(private s:AcademicsService){} @Get(':resource')list(@Param('resource')r:Resource,@Query()q:ListDto){return this.s.list(r,q,false)} @Post(':resource')create(@Param('resource')r:Resource,@Body()d:AcademicDto){return this.s.create(r,d)} @Patch(':resource/:id')update(@Param('resource')r:Resource,@Param('id')id:string,@Body()d:Partial<AcademicDto>){return this.s.update(r,id,d)}}
