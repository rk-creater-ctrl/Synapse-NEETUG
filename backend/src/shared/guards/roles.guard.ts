import { CanActivate, ExecutionContext, Injectable } from '@nestjs/common'; import { Reflector } from '@nestjs/core'; import { RoleName } from '@prisma/client'; import { ROLES_KEY } from '../decorators/roles.decorator'; @Injectable() export class RolesGuard implements CanActivate {constructor(private r:Reflector){} canActivate(c:ExecutionContext){const required=this.r.getAllAndOverride<RoleName[]>(ROLES_KEY,[c.getHandler(),c.getClass()]); if(!required?.length)return true; const user=c.switchToHttp().getRequest().user; return required.some(role=>user.roles?.includes(role))}}
