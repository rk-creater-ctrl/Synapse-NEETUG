'use client'; import {useState} from 'react'; export default function Login(){const[email,setEmail]=useState('');const[password,setPassword]=useState('');const[error,setError]=useState('');async function submit(e:React.FormEvent){e.preventDefault();const r=await fetch(`${process.env.NEXT_PUBLIC_API_URL}/auth/login`,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({email,password})});if(!r.ok)return setError('Login failed');localStorage.setItem('synapse_tokens',JSON.stringify(await r.json()));location.href='/dashboard'}return <div className="card"><h1>Admin login</h1><form onSubmit={submit} className="row"><input placeholder="Email" type="email" onChange={e=>setEmail(e.target.value)} required/><input placeholder="Password" type="password" onChange={e=>setPassword(e.target.value)} required/><button>Sign in</button></form>{error&&<p>{error}</p>}</div>}
